<UserControl x:Class="PeluqueriaAdmin.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:PeluqueriaAdmin.App.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="640"
             d:DesignWidth="960"
             Language="es-ES"
             mc:Ignorable="d">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
          <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
          <StackPanel>
            <TextBlock FontSize="14"
                       FontWeight="SemiBold"
                       Text="Fecha actual" />
            <TextBlock Margin="0,4,0,0"
                       FontSize="20"
                       Text="{Binding FechaActual}" />
          </StackPanel>
          <StackPanel Grid.Column="1" Orientation="Horizontal">
            <Grid Margin="0,0,10,0">
              <ToggleButton x:Name="MaintenanceBell" Width="46" Height="42" ToolTip="Mantenimientos para hoy o vencidos" AutomationProperties.Name="Notificaciones de mantenimiento" IsChecked="{Binding IsMaintenanceNotificationsOpen}">
                <Grid><Path Width="20" Height="22" Stretch="Uniform" Stroke="#334155" StrokeThickness="1.8" Data="M 4,16 L 16,16 L 14.5,13 L 14.5,8 A 4.5,4.5 0 0 0 5.5,8 L 5.5,13 Z M 8,18 A 2,2 0 0 0 12,18" /></Grid>
              </ToggleButton>
              <Border Margin="30,-5,-5,0" Padding="5,1" HorizontalAlignment="Right" VerticalAlignment="Top" Background="#DC2626" CornerRadius="9"><TextBlock Foreground="White" FontSize="11" Text="{Binding MaintenanceNotificationCount}" /></Border>
              <Popup PlacementTarget="{Binding ElementName=MaintenanceBell}" Placement="Bottom" StaysOpen="False" IsOpen="{Binding IsMaintenanceNotificationsOpen}">
                <Border Width="390" MaxHeight="330" Padding="12" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6">
                  <StackPanel><TextBlock Margin="0,0,0,8" FontWeight="SemiBold" Text="Mantenimientos pendientes" /><DataGrid MaxHeight="230" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding MaintenanceNotifications}"><DataGrid.Columns><DataGridTextColumn Width="*" Binding="{Binding Asset}" Header="Equipo" /><DataGridTextColumn Width="*" Binding="{Binding Type}" Header="Tipo" /><DataGridTextColumn Width="95" Binding="{Binding Date}" Header="Fecha" /><DataGridTextColumn Width="70" Binding="{Binding Status}" Header="Estado" /></DataGrid.Columns></DataGrid><Button Margin="0,8,0,0" Padding="10,5" HorizontalAlignment="Right" Command="{Binding GoToMaintenanceCommand}" Content="Ir a Mantenimiento" /></StackPanel>
                </Border>
              </Popup>
            </Grid>
            <Grid>
              <ToggleButton x:Name="ObligationBell" Width="46" Height="42" ToolTip="Obligaciones para hoy o vencidas" AutomationProperties.Name="Notificaciones de obligaciones" IsChecked="{Binding IsObligationNotificationsOpen}">
                <Grid><Path Width="20" Height="22" Stretch="Uniform" Stroke="#334155" StrokeThickness="1.8" Data="M 4,2 L 16,2 L 16,20 L 4,20 Z M 7,7 L 13,7 M 7,11 L 13,11 M 7,15 L 11,15" /></Grid>
              </ToggleButton>
              <Border Margin="30,-5,-5,0" Padding="5,1" HorizontalAlignment="Right" VerticalAlignment="Top" Background="#DC2626" CornerRadius="9"><TextBlock Foreground="White" FontSize="11" Text="{Binding ObligationNotificationCount}" /></Border>
              <Popup PlacementTarget="{Binding ElementName=ObligationBell}" Placement="Bottom" StaysOpen="False" IsOpen="{Binding IsObligationNotificationsOpen}">
                <Border Width="440" MaxHeight="330" Padding="12" Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6">
                  <StackPanel><TextBlock Margin="0,0,0,8" FontWeight="SemiBold" Text="Obligaciones pendientes" /><DataGrid MaxHeight="230" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding ObligationNotifications}"><DataGrid.Columns><DataGridTextColumn Width="*" Binding="{Binding Name}" Header="Nombre" /><DataGridTextColumn Width="95" Binding="{Binding Type}" Header="Tipo" /><DataGridTextColumn Width="95" Binding="{Binding Date}" Header="Fecha" /><DataGridTextColumn Width="110" Binding="{Binding Amount}" Header="Valor" /><DataGridTextColumn Width="70" Binding="{Binding Status}" Header="Estado" /></DataGrid.Columns></DataGrid><Button Margin="0,8,0,0" Padding="10,5" HorizontalAlignment="Right" Command="{Binding GoToObligationsCommand}" Content="Ir a Obligaciones" /></StackPanel>
                </Border>
              </Popup>
            </Grid>
          </StackPanel>
        </Grid>

        <GroupBox Grid.Row="2"
                  Padding="16"
                  Header="Servicios e impuestos pendientes">
            <TextBlock VerticalAlignment="Center"
                       Text="{Binding EstadoServiciosEImpuestos}"
                       TextWrapping="Wrap" />
        </GroupBox>

        <GroupBox Grid.Row="4"
                  Padding="16"
                  Header="Personas con pagos pendientes por uso del local">
            <TextBlock VerticalAlignment="Center"
                       Text="{Binding EstadoPersonasConPagosPendientes}"
                       TextWrapping="Wrap" />
        </GroupBox>

        <GroupBox Grid.Row="6"
                  Padding="16"
                  Header="Cantidad faltante para alcanzar el punto de equilibrio mensual">
            <TextBlock VerticalAlignment="Center"
                       Text="{Binding EstadoPuntoDeEquilibrio}"
                       TextWrapping="Wrap" />
        </GroupBox>

        <GroupBox Grid.Row="8"
                  Padding="16"
                  Header="Precio sugerido por silla para equilibrio mensual">
            <TextBlock Text="{Binding PrecioSugeridoPorSilla}"
                       TextWrapping="Wrap" />
        </GroupBox>
    </Grid>
</UserControl>
