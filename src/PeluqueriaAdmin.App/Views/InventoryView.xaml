<UserControl x:Class="PeluqueriaAdmin.App.Views.InventoryView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
 xmlns:viewModels="clr-namespace:PeluqueriaAdmin.App.ViewModels" d:DataContext="{d:DesignInstance Type=viewModels:InventoryViewModel, IsDesignTimeCreatable=False}"
 d:DesignHeight="760" d:DesignWidth="990" Language="es-ES" mc:Ignorable="d">
  <UserControl.Resources>
    <Style x:Key="FieldBox" TargetType="Control"><Setter Property="MinHeight" Value="36" /><Setter Property="Padding" Value="9,6" /></Style>
    <Style x:Key="FormLabel" TargetType="TextBlock"><Setter Property="Height" Value="42" /><Setter Property="Margin" Value="0,0,0,5" /><Setter Property="FontWeight" Value="SemiBold" /><Setter Property="Foreground" Value="#273449" /><Setter Property="TextWrapping" Value="Wrap" /><Setter Property="VerticalAlignment" Value="Bottom" /></Style>
  </UserControl.Resources>
  <Grid Margin="26" Background="#F5F7FA">
    <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
    <StackPanel Margin="0,0,0,14">
      <TextBlock FontSize="28" FontWeight="SemiBold" Text="Inventario" />
      <TextBlock Margin="0,5,0,0" Foreground="#5B6575" Text="Existencias, movimientos y operaciones en áreas separadas." />
    </StackPanel>
    <TabControl Grid.Row="1" SelectedIndex="0">
      <TabItem Header="Inventario">
        <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding CurrentInventory}" SelectedItem="{Binding SelectedCurrentRow}">
          <DataGrid.Columns><DataGridTextColumn Width="190" Binding="{Binding Name}" Header="Producto" /><DataGridTextColumn Width="200" Binding="{Binding Category}" Header="Categoría" /><DataGridTextColumn Width="120" Binding="{Binding CurrentQuantity}" Header="Cantidad" /><DataGridTextColumn Width="155" Binding="{Binding AverageUnitCost}" Header="Costo configurado" /><DataGridTextColumn Width="155" Binding="{Binding DefaultSalePrice}" Header="Precio de venta" /><DataGridTextColumn Width="300" ElementStyle="{StaticResource WrappingCellText}" Binding="{Binding Description}" Header="Descripción" /><DataGridTextColumn Width="155" Binding="{Binding LastUpdate}" Header="Última actualización" /></DataGrid.Columns>
        </DataGrid>
      </TabItem>
      <TabItem Header="Movimientos">
        <Grid Margin="8">
          <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
          <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <TextBlock Margin="0,8,8,0" FontWeight="SemiBold" Text="Periodo" />
            <ComboBox Width="185" Style="{StaticResource FieldBox}" ItemsSource="{Binding PeriodOptions}" SelectedItem="{Binding SelectedPeriod}" />
            <DatePicker Width="135" Margin="8,0,0,0" SelectedDate="{Binding CustomPeriodFrom}" Visibility="{Binding ShowCustomPeriod, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <DatePicker Width="135" Margin="8,0,0,0" SelectedDate="{Binding CustomPeriodThrough}" Visibility="{Binding ShowCustomPeriod, Converter={StaticResource BooleanToVisibilityConverter}}" />
          </StackPanel>
          <DataGrid Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding MovementHistory}" SelectedItem="{Binding SelectedMovementRow}">
            <DataGrid.Columns><DataGridTextColumn Width="110" Binding="{Binding Date}" Header="Fecha" /><DataGridTextColumn Width="190" Binding="{Binding Product}" Header="Producto" /><DataGridTextColumn Width="180" Binding="{Binding Type}" Header="Movimiento" /><DataGridTextColumn Width="120" Binding="{Binding QuantityIn}" Header="Entrada" /><DataGridTextColumn Width="120" Binding="{Binding QuantityOut}" Header="Salida" /><DataGridTextColumn Width="145" Binding="{Binding UnitCost}" Header="Costo unitario" /><DataGridTextColumn Width="145" Binding="{Binding TotalValue}" Header="Valor total" /><DataGridTextColumn Width="300" ElementStyle="{StaticResource WrappingCellText}" Binding="{Binding Description}" Header="Descripción" /></DataGrid.Columns>
          </DataGrid>
        </Grid>
      </TabItem>
      <TabItem Header="Agregar">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel Margin="14">
            <Border Padding="16" Background="White" BorderBrush="#D7DEE8" BorderThickness="1" CornerRadius="7">
              <StackPanel>
                <WrapPanel Margin="0,0,0,12"><TextBlock Margin="0,8,10,0" FontWeight="SemiBold" Text="Operación" /><ComboBox Width="250" Style="{StaticResource FieldBox}" ItemsSource="{Binding Editor.ActionOptions}" SelectedItem="{Binding Editor.SelectedAction}" /></WrapPanel>
                <WrapPanel DataContext="{Binding Editor}">
                  <StackPanel Width="235" Margin="0,0,14,12" Visibility="{Binding ShowPrimary, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding PrimaryLabel}" /><TextBox Text="{Binding PrimaryText, UpdateSourceTrigger=PropertyChanged}"><TextBox.Style><Style TargetType="TextBox" BasedOn="{StaticResource FieldBox}"><Style.Triggers><DataTrigger Binding="{Binding UsePrimarySelector}" Value="True"><Setter Property="Visibility" Value="Collapsed" /></DataTrigger></Style.Triggers></Style></TextBox.Style></TextBox><ComboBox DisplayMemberPath="Display" ItemsSource="{Binding EntityOptions}" SelectedItem="{Binding SelectedEntityOption}"><ComboBox.Style><Style TargetType="ComboBox" BasedOn="{StaticResource FieldBox}"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding UsePrimarySelector}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></ComboBox.Style></ComboBox></StackPanel>
                  <StackPanel Width="220" Margin="0,0,14,12" Visibility="{Binding ShowSecondary, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding SecondaryLabel}" /><ComboBox Style="{StaticResource FieldBox}" IsEditable="True" ItemsSource="{Binding SecondaryOptions}" Text="{Binding SecondaryText, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                  <StackPanel Width="190" Margin="0,0,14,12" Visibility="{Binding ShowExtra, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding ExtraLabel}" /><ComboBox Style="{StaticResource FieldBox}" IsEditable="True" ItemsSource="{Binding ExtraOptions}" Text="{Binding ExtraText, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                  <StackPanel Width="175" Margin="0,0,14,12" Visibility="{Binding ShowDate, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding DateLabel}" /><DatePicker Style="{StaticResource FieldBox}" SelectedDate="{Binding FormDate}" /></StackPanel>
                  <StackPanel Width="190" Margin="0,0,14,12" Visibility="{Binding ShowAmount, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding AmountLabel}" /><TextBox Style="{StaticResource FieldBox}" Text="{Binding AmountText, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                  <StackPanel Width="220" Margin="0,0,14,12" Visibility="{Binding ShowSecondaryAmount, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding SecondaryAmountLabel}" /><TextBox Style="{StaticResource FieldBox}" Text="{Binding SecondaryAmountText, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                  <StackPanel Width="170" Margin="0,0,14,12" Visibility="{Binding ShowQuantity, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="{Binding QuantityLabel}" /><TextBox Style="{StaticResource FieldBox}" Text="{Binding QuantityText, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                  <StackPanel Width="280" Margin="0,0,14,12" Visibility="{Binding ShowOptionalDescription, Converter={StaticResource BooleanToVisibilityConverter}}"><TextBlock Style="{StaticResource FormLabel}" Text="Descripción opcional" /><TextBox MinHeight="55" Padding="9,6" TextWrapping="Wrap" Text="{Binding OptionalDescriptionText, UpdateSourceTrigger=PropertyChanged}" /></StackPanel>
                </WrapPanel>
                <WrapPanel><Button Margin="0,0,9,8" Padding="15,8" Background="#2E5BFF" Foreground="White" Command="{Binding SaveCommand}" Content="Guardar operación" /><Button Margin="0,0,9,8" Padding="13,8" Command="{Binding LoadSelectedCommand}" Content="Editar selección" /><Button Margin="0,0,9,8" Padding="13,8" Command="{Binding SaveEditCommand}" Content="Guardar edición" /><CheckBox Margin="10,0,8,8" VerticalAlignment="Center" IsChecked="{Binding Editor.ConfirmDelete}" Content="Confirmo eliminar" /><Button Margin="0,0,0,8" Padding="13,8" Command="{Binding DeleteCommand}" Content="Eliminar selección" /></WrapPanel>
                <TextBlock Foreground="#0F766E" Text="{Binding Editor.StatusMessage}" TextWrapping="Wrap" /><TextBlock Foreground="#0F766E" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
              </StackPanel>
            </Border>
            <TextBlock Margin="0,16,0,7" FontSize="18" FontWeight="SemiBold" Text="Productos agregados recientemente" />
            <DataGrid Height="240" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" ItemsSource="{Binding RecentProducts}" SelectedItem="{Binding SelectedCurrentRow}">
              <DataGrid.Columns><DataGridTextColumn Width="200" Binding="{Binding Name}" Header="Producto" /><DataGridTextColumn Width="210" Binding="{Binding Category}" Header="Categoría" /><DataGridTextColumn Width="130" Binding="{Binding CurrentQuantity}" Header="Cantidad" /><DataGridTextColumn Width="160" Binding="{Binding AverageUnitCost}" Header="Costo" /><DataGridTextColumn Width="160" Binding="{Binding DefaultSalePrice}" Header="Precio" /><DataGridTextColumn Width="300" ElementStyle="{StaticResource WrappingCellText}" Binding="{Binding Description}" Header="Descripción" /></DataGrid.Columns>
            </DataGrid>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>
  </Grid>
</UserControl>
